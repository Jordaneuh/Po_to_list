<p style="color: green"><%= notice %></p>

<% content_for :title, "Users" %>

<h1>Users</h1>

<div id="users">
  <% @users.each do |user| %>
    <%= render user %>
    <p>
      <%= link_to "Show this user", user %>
    </p>
  <% end %>
</div>

<%= link_to "New user", new_user_path %>
<br>
<div class="col-md-2 p-4 bg-primary-subtle border border-primary-subtle">
        <%= link_to "Voir mes tâches", tasks_path %>
</div>